#[cfg(test)]
mod tests {
    #[tokio::test]
    async fn test_gatt_application_register() {
        // let conn = std::sync::Arc::new(bluebus::get_system_connection().await.unwrap());
        // bluebus::register_gatt_application(&conn).await.unwrap();
        // bluebus::unregister_gatt_application(&conn).await.unwrap();
        assert!(true);
    }
}
