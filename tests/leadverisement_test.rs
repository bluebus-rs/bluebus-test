#[cfg(test)]
mod tests {
    #[tokio::test]
    async fn test_leadvertisement_register_unregister() {
        // let conn = std::sync::Arc::new(bluebus::get_system_connection().await.unwrap());
        // bluebus::register_advertisement(&conn).await.unwrap();
        // bluebus::unregister_advertisement(&conn).await.unwrap();
        assert!(true);
    }
}
